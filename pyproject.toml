[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis-rag"
version = "0.1.0"
description = "A modular multi-source RAG system for developer knowledge"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]